<UserControl
    x:Class="Warehouse.Silverlight.NavigationModule.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:Warehouse.Silverlight.NavigationModule.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=viewModels:LoginViewModel}">

    <Grid Width="300" Margin="0,300,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Имя пользователя: " HorizontalAlignment="Right" />
        <TextBox Grid.Column="1" Text="{Binding Login, Mode=TwoWay}" />

        <TextBlock Text="Пароль: " Grid.Row="1" HorizontalAlignment="Right" />
        <PasswordBox Grid.Row="1" Grid.Column="1" Password="{Binding Password, Mode=TwoWay}" />

        <Button Grid.Column="1" Grid.Row="2" Content="Войти" HorizontalAlignment="Left" Command="{Binding LoginCommand}" />
    </Grid>

</UserControl>
