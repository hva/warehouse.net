<UserControl
    x:Class="Warehouse.Silverlight.NavigationModule.Views.TopMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:Warehouse.Silverlight.Controls"
    xmlns:viewModels="clr-namespace:Warehouse.Silverlight.ViewModels"
    mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:TopMenuViewModel}">

    <Grid Height="45">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <controls:TopMenuDivider />
        <controls:TopMenuItem Grid.Column="1" Content="Остатки" IsChecked="True" />
        <controls:TopMenuDivider Grid.Column="2" />

        <controls:TopMenuDivider Grid.Column="4" />
        <controls:TopMenuItem Grid.Column="5" Content="{Binding LogoutContent}" Command="{Binding LogoutCommand}" />
        <controls:TopMenuDivider Grid.Column="6" />
    </Grid>
</UserControl>
